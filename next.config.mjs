/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		dangerouslyAllowSVG: true,
		// localPatterns: [
		// 	{
		// 		pathname: '/public',
		// 		search: '',
		// 	}
		// ],
	}
};

export default nextConfig;